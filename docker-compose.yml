services:
  audiobooknotes:
    build: .
    container_name: audiobooknotes
    restart: unless-stopped
    ports:
      - "7777:7777"
    volumes:
      - ./data:/data
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
